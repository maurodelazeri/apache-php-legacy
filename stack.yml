version: "3"
services:
  bb:
    image: bb:latest
    restart: always
    ports:
      - 8080:80
    volumes:
      - ./admin7.dineroloco.com:/opt/admin7.dineroloco.com
      - ./000-project.conf:/etc/apache2/sites-enabled/000-project.conf
